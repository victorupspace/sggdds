# Raw Tokens

Coloque aqui arquivos exportados do Figma com sufixo `.tokens.json`.

O arquivo `core-sggd.tokens.json` contem os Core tokens importados do Figma. Arquivos auxiliares do macOS, como `._*`, nao devem ser versionados.
